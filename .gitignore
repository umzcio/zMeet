.build/
.swiftpm/
.DS_Store

# Local runtime state belongs in ~/.zmeet, not the source repo.
.zmeet/

# Generated editor files
*.xcuserstate
*.xcworkspace

# Built app bundle output
build/

# Notary credentials (local only)
scripts/.notary-config.local

# Internal specs/plans/mocks — not published (RELEASING.md is tracked)
docs/*
!docs/RELEASING.md
